// Haggler Skill — thin wrapper around the MCP server
// When installed as an OpenClaw skill, this starts the MCP server
export { } // This re-exports the MCP server entry point

// The skill is distributed as an MCP server.
// The SKILL.md manifest tells OpenClaw how to install and invoke it.
// Actual server logic lives in @haggler/mcp-server
